import java.util.*;

public class LoginManager
{
  private final int driverID;
  public LoginManager(int givenDriverId)
  {
    driverID=givenDriverId; 
    if(idValid())
  } 
  public boolean idValid()
  { 
    //int check = DriverInfo.findDriver(driverID);
    //for(int i = 0;i < 70;
  } 
}
